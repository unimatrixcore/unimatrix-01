# Example production browser runtime config for a separate-origin deployment.
# Same-origin deployments can keep `VITE_API_BASE_URL=/api` instead.
# Replace `api.unimatrix-01.dev` with the omnimatrix.dev API host later if that domain becomes primary.
VITE_API_BASE_URL=https://api.unimatrix-01.dev
